const Actor = require('./actor').Actor
const ContentRating = require('./content-rating').ContentRating
const Genre = require('./genre').Genre
const Producer = require('./producer').Producer

module.exports = {
    actor:Actor,
    contentRating:ContentRating,
    genre:Genre,
    producer:Producer
}